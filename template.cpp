#include <iostream>
#include <string>
#include <sstream>
#include <cstring>
#include <iomanip>
#include <cmath>
#include <algorithm>
#include <tuple>
#include <vector>
#include <queue>
#include <stack>
#include <deque>
#include <list>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>

#include "split.h"

using namespace std;

int main() {
    long ans = 0;

    string input;
    while(getline(cin, input)) {
    }

    string input;
    getline(cin, input);
    do {
        getline(cin, input);
    } while(!input.empty());

    long input;
    while(cin >> input) {
    }

    cout << ans << endl;
    return 0;
}

