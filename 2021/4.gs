n.+%
(','%{~}%:a\
{n%{' '%{~}%}%}%:b\
0@@
{:x;.{{{{.x={100+}{}if}%}%}%.{{{99>}%}%}%.{zip}%{{{{&}*}%{|}*}%}:f[~]\[f]+zip{~|}%1?.0<!{={{.99>{;0}{}if}%{+}*}%{+}*x*p}{;}if}{}if}/

b a
{:x;.{
{{{.x={100+}{}if}%}%}%
  .{{{99>}%}%}%
  .{zip}%
  {{{{&}*}%{|}*}%}:f
  [~]\[f]+zip{~|}%
  .{0=},,.

  1=
  {;0?:y;}
  {
    0=
    {;y={{.99>{;0}{}if}%{+}*}%{+}*x*p}
    {;}
    if
  }
  if
}{}if}/
;
